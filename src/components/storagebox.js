import React from 'react';
import SubmitTab from './submittab';

class StorageBox extends React.Component {
    render() {
        return (
            <div className="storage-box">
                <SubmitTab></SubmitTab>
                
            </div>
        );
    }
} // + findtab after submittab

export default StorageBox;